export default function Footer() {
  return (
    <footer className="max-w-3xl mx-auto px-4 py-8 text-xs text-slate-500">
      <p>© {new Date().getFullYear()} — Provider App.</p>
    </footer>
  );
}
